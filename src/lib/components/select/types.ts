/* import type { SelectOption, createSelect } from '@melt-ui/svelte';
import type { Writable } from 'svelte/store';

type CreateTabs = ReturnType<typeof createSelect>;
type Elements = CreateTabs['elements'];

export type SelectContext = Pick<Elements, 'trigger' | 'label' | 'menu' | 'option'> & {
	options: Writable<SelectOption[]>;
};
 */
